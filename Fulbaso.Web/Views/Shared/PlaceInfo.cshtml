@using Fulbaso.Contract;
@using Fulbaso.Web;

@model Place

<fieldset>
    <dl>
        @if (Model.Address != null)
        {
            <dt>Dirección</dt>
            <dd>@Model.FullAddress</dd>
        }
        @if (Model.Phone != null)
        {
            <dt>Teléfono</dt>
            <dd>@Model.Phone</dd>
        }
        <dt>Canchas</dt>
        <dd id="courtscount">@Model.Courts</dd>
    </dl>
</fieldset>
