# Build Folders (you can keep bin if you'd like, to store dlls and pdbs)
bin
obj

# upload folder
uploaded

# mstest test results
TestResults

# not a build folder per se, but the NuGet packages repository
packages

# local environment configuration files
*.suo
*.user
desktop.ini